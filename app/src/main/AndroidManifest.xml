<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.feleves_projekt" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Projekt_exp_4"
        android:usesCleartextTraffic="true" >
        <activity
            android:name="com.example.feleves_projekt.ReservationEditActivity"
            android:exported="true"
            android:label="@string/reservations_edit_title" />
        <activity
            android:name="com.example.feleves_projekt.ReservationsActivity"
            android:exported="true"
            android:label="@string/reservations_list_title" />
        <activity
            android:name="com.example.feleves_projekt.MainActivity"
            android:exported="true"
            android:label="@string/wellcome_title" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>